{{- if .Values.backend2.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backend2.name }}
  labels:
    app: {{ .Values.backend2.name }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Values.backend2.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.backend2.name }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Values.backend2.name }}
          image: "{{ .Values.backend2.image.repository }}:{{ .Values.backend2.image.tag }}"
          imagePullPolicy: {{ .Values.backend2.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.backend2.port }}
          env:
            {{- range $key, $value := .Values.backend2.env }}
            - name: {{ $key }}
              value: "{{ $value }}"
            {{- end }}
          resources:
            {{- toYaml .Values.backend2.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
      volumes:
        - name: tmp-volume
          emptyDir: {}
{{- end }}

